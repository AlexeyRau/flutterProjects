name: study_flow
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  icalendar_parser: ^2.1.0
  http: ^1.3.0
  intl: ^0.20.2
  flutter_markdown: ^0.7.6+2
  flutter_dotenv: ^5.2.1

  
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0


flutter:
  uses-material-design: true
  
  # Добавьте эту секцию для включения .env и других ресурсов
  assets:
    - .env  # Указываем файл .env как ассет